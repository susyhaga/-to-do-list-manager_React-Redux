export enum Prioridade {
  URGENTE = 'urgente',
  IMPORTANTE = 'importante',
  NORMAL = 'normal'
}

export enum Status {
  PENDENTE = 'pendente',
  CONCLUIDA = 'concluida'
}

//exportar ENUNS para o estilo e o index do componente Tarefa + reducers/tarefa.ts
//container/ListaDeTerefas + models/Tarefa.ts
